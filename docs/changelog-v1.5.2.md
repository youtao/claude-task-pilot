# 更新日志 v1.5.2

**发布日期**: 2026-02-01

---

## 新增功能

### ✨ 任务完成命令 (`/complete-task`)

新增 `/complete-task` 命令，解决"工作完成后文档没有自动更新"的问题。

**使用方式**:
```bash
# 使用当前任务（从 session.md 读取）
/complete-task

# 指定任务ID
/complete-task task-001

# 指定文件路径
/complete-task task-001-feature.md
```

**自动执行的操作**:
- ✅ 移动任务卡片到归档目录 (`docs/done/YYYY-MM/`)
- ✅ 创建完成报告
- ✅ 更新 `session.md` - 清空"当前任务"，添加到"上一个任务"
- ✅ 更新 `current-sprint.md` - 将任务状态改为 ✅ 完成
- ✅ 更新 `archive-index.md` - 添加归档记录
- ✅ 推荐下一个任务 - 调用 task-suggester agent

**解决的问题**:
- 用户在 Claude Code 中完成工作后，文档没有自动更新
- 需要手动更新多个文档，操作繁琐
- 容易遗漏更新某个文档

**使用场景**:
1. Claude Code 按照 session.md 中的计划完成工作
2. 运行 `/complete-task` 标记任务完成
3. 所有相关文档自动更新
4. 自动推荐下一个任务

### 📚 新增文档

- **[commands/complete-task.md](../commands/complete-task.md)** - 任务完成命令详细说明
- **[docs/task-completion-workflow.md](task-completion-workflow.md)** - 任务完成工作流程指南
- **[docs/test-bash-mv-detection.md](test-bash-mv-detection.md)** - Bash mv 命令检测测试文档

---

## 改进

### 🔧 PostToolWrite Hook 增强

- 增加 Bash 工具监听，支持检测 `mv` 命令
- 支持多种 mv 命令格式（直接路径、引号包裹、git mv）
- 新增命令解析逻辑，自动提取任务信息

### 🔧 SessionStart Hook 增强

- 增加未处理任务检测逻辑
- 扫描 `done/` 目录，发现未记录的任务
- 自动补充更新相关文档

---

## 修复

### 🐛 修复: Bash mv 命令不触发文档更新

**问题描述**:
使用 `mv` 命令移动任务卡片时，PostToolWrite Hook 不会被触发，导致文档不更新。

**修复方案**:
1. 在 `ifTool` 中增加 `Bash` 工具监听
2. 添加 mv/git mv 命令解析逻辑
3. 标准化文件路径处理

**测试验证**:
详见 [docs/test-bash-mv-detection.md](test-bash-mv-detection.md)

---

## 向后兼容性

✅ 完全向后兼容，不影响现有功能
✅ 新命令为可选使用，原有工作流程不变
✅ Hook 增强不影响现有逻辑

---

## 升级指南

### 从 v1.5.0/v1.5.1 升级

无需特殊操作，直接升级即可：

```bash
cd ~/.claude/plugins/claude-task-pilot
git pull origin master
```

### 推荐工作流程变更

**之前**:
```bash
# 1. 完成工作
mv docs/todo/backlog/task-001.md docs/done/2026-02/
# 2. Hook 自动更新（但有时不触发）
# 3. 手动检查文档是否更新
```

**现在**（推荐）:
```bash
# 1. 完成工作
/complete-task
# 2. 所有文档自动更新，推荐下一个任务
```

---

## 已知问题

### 限制

1. **通配符移动不支持**:
   ```bash
   # ❌ 不支持
   mv docs/todo/backlog/*.md docs/done/2026-02/
   ```

2. **复杂管道不支持**:
   ```bash
   # ❌ 不支持
   cat task-001.md | ... > docs/done/2026-02/task-001.md
   ```

3. **使用 cp 复制不支持**:
   ```bash
   # ❌ 不支持（不是移动操作）
   cp docs/todo/backlog/task-001.md docs/done/2026-02/
   ```

### 解决方案

对于以上场景，请使用 `/complete-task` 命令：
```bash
/complete-task task-001
```

---

## 下一步计划

### v1.6.0 计划

- [ ] Web Dashboard - 可视化任务管理界面
- [ ] 任务时间追踪 - 记录任务实际耗时
- [ ] 团队协作支持 - 多人任务分配
- [ ] GitHub Issues 集成 - 双向同步
- [ ] 自定义评分算法 - 可配置推荐逻辑

---

## 贡献者

- **youtao** - 主要作者

---

## 反馈与支持

- **GitHub Issues**: [提交问题](https://github.com/youtao/claude-task-pilot/issues)
- **讨论区**: [GitHub Discussions](https://github.com/youtao/claude-task-pilot/discussions)

---

**下载**: [v1.5.2 Release](https://github.com/youtao/claude-task-pilot/releases/tag/v1.5.2)
