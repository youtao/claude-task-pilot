---
description: 自动生成每日开发进度报告
color: "10B981"
---

# Daily Reporter Agent - 日报生成

## 功能描述

收集当日开发活动，生成结构化的每日进度报告。报告包括：
- 完成的任务
- 新建的任务
- 代码变更统计
- Git 提交记录
- 明日计划建议

---

## 触发时机

1. **每日首次启动**: SessionStart 时检测日期变化
2. **用户主动触发**: `/generate-daily-report` 命令
3. **任务完成后**: 可选，生成任务完成报告的一部分

---

## 数据收集

### 步骤 1: 确定报告日期

```javascript
const today = new Date().toISOString().slice(0, 10)  // "2026-01-30"
const yesterday = new Date(Date.now() - 86400000).toISOString().slice(0, 10)
```

### 步骤 2: 收集任务变更

```markdown
**从以下文件提取**:
1. `$DOCS_ROOT/session.md` - 读取"每日进度"部分
2. `$DOCS_ROOT/todo/current-sprint.md` - 读取任务状态变化
3. `$DOCS_ROOT/done/` - 检查是否有新归档的任务
```

**提取逻辑**:
```javascript
// 查找今日完成的任务
const completedTasks = parseDailyProgress(sessionMd, today).filter(
  task => task.status === "✅ 完成"
)

// 查找今日新建的任务
const newTasks = parseDailyProgress(sessionMd, today).filter(
  task => action === "创建任务"
)

// 查找状态变化的任务
const statusChanges = parseSprintChanges(currentSprintMd, yesterday, today)
```

### 步骤 3: 收集代码变更

**Git 统计**:
```bash
# 今日提交
git log --since="$(date -d 'today' +%Y-%m-%dT00:00:00)" \
        --until="$(date -d 'today' +%Y-%m-%dT23:59:59)" \
        --pretty=format:"- %s" \
        --stat

# 文件变更统计
git diff --stat "$(git rev-parse -q --verify HEAD~1)" HEAD
```

**提取信息**:
- 提交次数
- 修改的文件列表
- 代码行数变化（+/-）
- 主要修改模块

### 步骤 4: 分析活动类型

```javascript
const activities = {
  tasksCompleted: completedTasks.length,
  tasksStarted: statusChanges.filter(t => t.from === "⏳ 待开始" && t.to === "🔄 进行中"),
  tasksCreated: newTasks.length,
  commits: commitCount,
  filesModified: modifiedFiles.length,
  linesAdded: addedLines,
  linesDeleted: deletedLines
}
```

---

## 报告生成

### 输出格式

```markdown
## 📊 {{DATE}} 开发日报

### 📈 今日概览

- **完成任务**: {{COMPLETED_COUNT}} 个
- **新建任务**: {{NEW_TASKS_COUNT}} 个
- **进行中任务**: {{IN_PROGRESS_COUNT}} 个
- **代码提交**: {{COMMIT_COUNT}} 次
- **修改文件**: {{FILES_MODIFIED}} 个

---

### ✅ 完成任务

{{#each completedTasks}}
- [task-{{id}}] {{name}}
  - 完成时间: {{completion_time}}
  - 关键成果: {{key_achievements}}
{{/each}}

---

### 🆕 新建任务

{{#each newTasks}}
- [task-{{id}}] {{name}}
  - 创建时间: {{creation_time}}
  - 优先级: {{priority}}
{{/each}}

---

### 💻 代码变更

**提交记录** ({{COMMIT_COUNT}} 次):
{{#each commits}}
- {{commit_time}} {{commit_message}}
  - 文件: {{files_changed}}
{{/each}}

**修改文件统计**:
{{#each file_changes}}
- \`{{file_path}}\`: +{{added}} -{{deleted}}
{{/each}}

**主要变更模块**:
- {{module_1}}: {{changes_count}} 处
- {{module_2}}: {{changes_count}} 处

---

### 📋 任务进度

**本周冲刺进度**: {{SPRINT_PROGRESS}}%

- [x] {{COMPLETED_TASKS_COUNT}} 个已完成
- [ ] {{IN_PROGRESS_TASKS_COUNT}} 个进行中
- [ ] {{PENDING_TASKS_COUNT}} 个待开始

---

### 🎯 明日计划

基于当前进度，建议明日完成：

{{#each next_priority_tasks}}
1. [task-{{id}}] {{name}}
   - 理由: {{reason}}
   - 预计时间: {{estimated_time}}
{{/each}}

---

### 💡 技术决策

今日做出的技术决策：

{{#each tech_decisions}}
- **{{title}}**: {{description}}
  - 理由: {{rationale}}
{{/each}}

---

### 🚧 阻塞问题

当前遇到的阻塞问题：

{{#if has_blocking_issues}}
{{#each blocking_issues}}
- **{{issue}}**: {{description}}
  - 影响范围: {{impact}}
  - 解决方案: {{solution}}
{{/each}}
{{else}}
✅ 今日无阻塞问题
{{/if}}

---

### 📝 备注

{{NOTES}}

---

**报告生成时间**: {{GENERATION_TIME}}
**报告版本**: v1.0
```

---

## 保存位置

### 选项 A: 保存到 daily/ 目录

```
$DOCS_ROOT/daily/2026-01/2026-01-30.md
```

**优点**: 便于历史追溯，形成完整的开发日志

### 选项 B: 追加到 session.md

在 `session.md` 的"## 📅 每日进度"部分添加新日期

**优点**: 集中管理，减少文件数量

### 选项 C: 不保存，仅显示

**优点**: 减少文件系统操作

**推荐**: **选项 A**（保存到 daily/），便于长期追溯

---

## 自动化集成

### 在 SessionStart 中触发

```markdown
**检测逻辑**:
```javascript
const today = new Date().toISOString().slice(0, 10)
const lastReportDate = getLastReportDate()  // 从 daily/ 或 session.md 读取

if (today !== lastReportDate) {
  // 日期变化，生成昨日报表
  await generateDailyReport(lastReportDate)

  // 显示今日摘要
  await showTodaySummary()
}
```

### 在任务完成时触发

```markdown
**可选功能**: 完成任务后询问是否生成日报

```javascript
// 在 PostToolWrite hook 中
if (isTaskCompleted()) {
  const shouldGenerate = await askUser("是否生成今日日报？")

  if (shouldGenerate) {
    await generateDailyReport(today)
  }
}
```

---

## 明日计划生成

**逻辑**: 调用 `task-suggester` agent

```javascript
const recommendations = await suggestNextTasks()

// 选取前 3 个作为明日计划
const tomorrowPlan = recommendations.slice(0, 3)
```

---

## 配置参数

```yaml
save_location: "daily"  # "daily" | "session" | "none"
daily_dir: "daily"  # 日报保存目录
include_commits: true  # 是否包含 Git 提交记录
include_file_stats: true  # 是否包含文件变更统计
auto_generate: true  # 是否自动生成（每日首次启动时）
```

---

## 示例输出

```markdown
## 📊 2026-01-30 开发日报

### 📈 今日概览

- **完成任务**: 1 个
- **新建任务**: 2 个
- **进行中任务**: 3 个
- **代码提交**: 5 次
- **修改文件**: 8 个

---

### ✅ 完成任务

- [task-002] 收集番茄种植专业资料
  - 完成时间: 2026-01-30 18:30
  - 关键成果:
    - 收集 3 本番茄种植专业书籍
    - 整理 20+ 篇网络教程
    - 设计知识库数据结构

---

### 🆕 新建任务

- [task-003] 补充番茄品种数据
  - 创建时间: 2026-01-30 19:00
  - 优先级: P0

- [task-004] 补充番茄生长阶段数据
  - 创建时间: 2026-01-30 19:15
  - 优先级: P0

---

### 💻 代码变更

**提交记录** (5 次):
- 18:00 chore: 更新 session.md
  - 文件: docs/session.md
- 18:05 feat: 添加番茄品种数据结构
  - 文件: backend/Domain/TomatoVariety.cs
- 18:20 docs: 创建任务卡片 task-003
  - 文件: docs/todo/backlog/task-003.md
- 18:35 feat: 实现品种数据 CRUD
  - 文件: backend/Application/...
- 18:50 refactor: 优化数据库查询
  - 文件: backend/EntityFrameworkCore/...

**修改文件统计**:
- `backend/Domain/TomatoVariety.cs`: +120 -15
- `docs/session.md`: +45 -12
- `docs/todo/backlog/task-003.md`: +68 -0

**主要变更模块**:
- backend: 4 处
- docs: 4 处

---

### 📋 任务进度

**本周冲刺进度**: 30% (3/10)

- [x] 1 个已完成
- [ ] 3 个进行中
- [ ] 6 个待开始

---

### 🎯 明日计划

基于当前进度，建议明日完成：

1. [task-003] 补充番茄品种数据
   - 理由: 高优先级（P0），无依赖任务
   - 预计时间: 2-3 小时

2. [task-004] 补充番茄生长阶段数据
   - 理由: 同样是 P0，可并行准备
   - 预计时间: 1-2 小时

3. [task-005] 设计RAG知识库架构
   - 理由: 依赖任务（task-003, 004）完成后可立即开始
   - 预计时间: 3-4 小时

---

### 💡 技术决策

今日做出的技术决策：

- **知识库存储方案**: 选择 pgvector 而非 Qdrant
  - 理由: 已有 PostgreSQL 基础设施，减少技术栈复杂度

---

### 🚧 阻塞问题

✅ 今日无阻塞问题

---

### 📝 备注

- 番茄种植资料已整理完成，等待数据录入
- 品种数据模板已设计，可直接开始填充
- 建议明日先完成数据准备工作，再开始 RAG 架构设计

---

**报告生成时间**: 2026-01-30 23:59
**报告版本**: v1.0
```

---

## 测试场景

1. **正常生成**: 有完成任务和代码提交
2. **空日报**: 无任何活动（应显示"今日无进展"）
3. **首次使用**: 无历史记录（应初始化）
4. **跨日期**: 昨晚工作到今天凌晨（应正确处理）
5. **无 Git**: 项目未初始化 Git（应跳过 Git 相关部分）

---

## 性能优化

- **异步收集**: Git 命令并行执行
- **缓存结果**: 避免重复查询同一天的数据
- **增量生成**: 只收集变化的部分
- **响应时间**: < 2 秒
