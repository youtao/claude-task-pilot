# Claude Task Pilot - AI 驱动的任务管理系统

## 📖 项目概述

**Claude Task Pilot** 是一个专为 Claude Code 设计的自动化任务管理插件，通过文件系统监控和智能推荐，为 AI 驱动的开发流程提供无缝的任务追踪和状态管理。

### 核心特性

- **AI 原生设计**: 文档结构化、高信息密度，优化 Claude Code 的理解能力
- **自动化追踪**: 通过 Hooks 自动监控任务状态变化，无需手动更新
- **智能推荐**: 基于优先级、依赖关系和里程碑自动推荐下一个任务
- **快速启动**: 5-10 秒快速了解当前项目状态
- **文档驱动**: 基于文件系统的简单、透明设计

---

## 🏗️ 项目结构

```
claude-task-pilot/
├── .claude-plugin/
│   └── plugin.json              # 插件配置清单
├── agents/
│   ├── task-suggester.md        # 智能任务推荐代理
│   └── daily-reporter.md        # 日报生成代理
├── hooks/
│   ├── session-start.md         # Session 启动钩子
│   ├── pre-tool-write.md        # 工具写入前钩子
│   └── post-tool-write.md       # 工具写入后钩子
├── templates/
│   ├── session.md               # Session 状态模板
│   ├── roadmap.md               # 长期路线图模板
│   ├── current-sprint.md        # 当前冲刺模板
│   ├── archive-index.md         # 归档索引模板
│   └── task-complete.md         # 任务完成报告模板
├── CLAUDE.md                     # 本文档
├── README.md                     # 用户文档
└── .gitignore                    # Git 忽略规则
```

---

## 🎯 核心概念

### 文档结构

插件在用户项目中创建以下结构：

```
docs/
├── session.md              # 当前 Session 状态（<80 行）
├── todo/
│   ├── roadmap.md          # 长期路线图（3-6 个月）
│   ├── current-sprint.md   # 当前冲刺（1-2 周）
│   └── backlog/            # 任务卡片
│       └── task-XXX-*.md
└── done/                   # 已完成任务归档
    ├── archive-index.md    # 归档索引
    └── YYYY-MM/            # 按月归档
        └── task-XXX-*.md
```

### 任务卡片格式

```markdown
# task-003: 补充番茄品种数据

**创建时间**: 2026-01-30
**优先级**: P0
**模块**: backend
**里程碑**: Phase 2: AI 核心功能

## 任务描述
补充 50 个常见番茄品种数据...

## 验收标准
- [ ] 至少 50 个品种
- [ ] 每个品种包含完整信息

## 依赖任务
- task-002: 收集番茄种植专业资料 ✅
```

### 优先级系统

- **P0**: 紧急重要，必须立即处理
- **P1**: 重要但不紧急，本周内完成
- **P2**: 可以延后，有空时处理

---

## 🔧 架构设计

### Hooks 触发流程

```
SessionStart Hook
    ↓
检测项目状态
    ↓
├─ 未初始化 → 提示初始化
├─ session.md 丢失 → 自动重建
└─ 正常启动 → 显示任务摘要

PreToolWrite Hook
    ↓
监控新任务卡片创建
    ↓
自动更新 session.md 和 current-sprint.md

PostToolWrite Hook
    ↓
监控任务归档
    ↓
├─ 创建完成报告
├─ 更新索引
├─ 触发 task-suggester agent
└─ 规范守护（session.md < 80 行）
```

### Agent 调用

#### task-suggester
**触发时机**:
- 任务完成后
- 用户主动询问
- 每日启动（可选）

**推荐逻辑**:
1. 优先级评分（P0: 100, P1: 70, P2: 40）
2. 依赖关系检查（依赖任务必须完成）
3. 里程碑对齐（当前里程碑优先）
4. 模块平衡（避免连续做同一模块）

#### daily-reporter
**触发时机**:
- 每日首次启动

**报告内容**:
- 昨日完成任务
- 今日推荐任务
- 本周进度概览

---

## 💡 设计原则

### 1. AI 优先
- 文档使用结构化 Markdown
- 高信息密度，减少 Claude 的 token 消耗
- 明确的状态标记（✅、🔄、⏳）

### 2. 快速启动
- session.md 限制 80 行，快速读取
- 关键信息在前 20 行
- 5-10 秒了解项目状态

### 3. 任务驱动
- 所有开发活动围绕任务卡片
- 任务卡片是真理的唯一来源
- 完成即归档，不可修改

### 4. 完整追溯
- 任务生命周期完整记录
- 按月归档，便于回顾
- archive-index.md 提供全局视图

### 5. 非侵入式
- Hooks 只观察，不干扰用户操作
- 自动化但不强制
- 失败时不影响正常开发

---

## 🚀 使用指南

### 安装

```bash
# 克隆到插件目录
cd ~/.claude/plugins
git clone https://github.com/your-username/claude-task-pilot.git

# 或手动复制
cp -r claude-task-pilot ~/.claude/plugins/
```

### 配置

**项目级配置** (`.claude/claude-task-pilot.local.md`):

```yaml
---
docs_root: "docs"
session_max_lines: 80
auto_recommend: true
daily_report: true
---
```

### 初始化项目

新项目首次启动时，插件会自动提示：

```
🚀 检测到新项目

此项目尚未建立任务管理系统。是否立即初始化？

将会创建:
- docs/session.md
- docs/todo/roadmap.md
- docs/todo/current-sprint.md
- docs/done/archive-index.md
- docs/todo/backlog/
- docs/done/YYYY-MM/
```

### 创建任务

在 `docs/todo/backlog/` 创建新任务卡片：

```bash
# 示例
cat > docs/todo/backlog/task-004-design-api.md << 'EOF'
# task-004: 设计 REST API

**创建时间**: 2026-01-30
**优先级**: P1
**模块**: backend

## 任务描述
设计用户认证和数据管理的 REST API...

## 验收标准
- [ ] API 文档完整
- [ ] 包含错误处理
- [ ] 通过安全审查
EOF
```

**自动触发**:
- ✅ 更新 `session.md` "当前任务"
- ✅ 更新 `current-sprint.md` 状态为 🔄

### 完成任务

移动任务卡片到归档目录：

```bash
mv docs/todo/backlog/task-004-design-api.md docs/done/2026-01/
```

**自动触发**:
- ✅ 创建完成报告
- ✅ 更新 `session.md` "上一个任务"
- ✅ 更新 `current-sprint.md` 状态为 ✅
- ✅ 更新 `archive-index.md`
- ✅ 智能推荐下一个任务

---

## 🛠️ 开发指南

### 添加新 Hook

1. 在 `hooks/` 目录创建 `.md` 文件
2. 添加 YAML frontmatter
3. 在 `plugin.json` 中声明

```markdown
---
when: SessionEnd
description: 清理临时文件
---

# SessionEnd Hook - 清理临时文件
...
```

### 添加新 Agent

1. 在 `agents/` 目录创建 `.md` 文件
2. 定义触发条件和分析逻辑
3. 在 `plugin.json` 中声明

### 修改模板

模板文件位于 `templates/` 目录，修改后自动生效。

建议：
- 保持简洁，信息密度高
- 使用明确的 emoji 标记
- 包含使用示例

---

## 🧪 测试

### 手动测试场景

```bash
# 1. 新项目初始化
cd /tmp/test-project
claude-code
# 应该提示初始化

# 2. 创建任务
cat > docs/todo/backlog/task-001.md
# 检查 session.md 是否自动更新

# 3. 完成任务
mv docs/todo/backlog/task-001.md docs/done/2026-01/
# 检查是否生成完成报告和推荐

# 4. 重建 session.md
rm docs/session.md
claude-code
# 应该自动重建
```

### 自动化测试（待实现）

```bash
npm test
```

---

## 📊 性能指标

- **启动时间**: < 100ms
- **推荐计算**: < 500ms
- **文件监控**: 零延迟（基于 Hooks）
- **内存占用**: 最小化（无持久化进程）

---

## ⚠️ 注意事项

### 限制

1. **session.md 行数限制**: 超过 80 行会自动归档旧任务
2. **文件格式**: 必须使用 Markdown 格式
3. **目录结构**: 不能自定义目录结构（可通过配置修改路径）
4. **并发处理**: 不支持多个 Claude Code 实例同时写入

### 已知问题

- [ ] 移动任务卡片后，如果 Hook 未触发，需要手动更新索引
- [ ] 依赖关系解析基于字符串匹配，可能出现误判
- [ ] 模块平衡逻辑过于简单

### 故障排查

**问题**: 插件未加载

```bash
# 检查插件目录
ls ~/.claude/plugins/claude-task-pilot

# 检查 plugin.json 语法
cat ~/.claude/plugins/claude-task-pilot/.claude-plugin/plugin.json | jq
```

**问题**: Hook 未触发

```bash
# 检查 Hook 文件格式
head -5 hooks/session-start.md

# 应该看到 YAML frontmatter
```

**问题**: Agent 调用失败

- 检查 Agent 文件是否存在
- 检查 `plugin.json` 中的 agent 声明
- 查看 Claude Code 日志

---

## 🔄 版本历史

### v1.0.0 (2026-01-30)

**初始版本**
- ✅ 自动初始化
- ✅ 任务状态追踪
- ✅ 智能任务推荐
- ✅ 日报生成
- ✅ 规范守护

### 计划功能

- [ ] Web Dashboard
- [ ] 任务时间追踪
- [ ] 团队协作支持
- [ ] GitHub Issues 集成
- [ ] 自定义评分算法

---

## 🤝 贡献指南

欢迎贡献！请遵循以下流程：

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'Add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 开启 Pull Request

### 代码规范

- 所有文档使用 Markdown
- Hook 文件必须包含 YAML frontmatter
- Agent 文件必须包含示例对话
- 模板文件必须简洁明了

---

## 📄 许可证

MIT License - 详见 LICENSE 文件

---

## 👥 维护者

**youtao** - GitHub: [@your-username](https://github.com/your-username)

---

## 🙏 致谢

- [Claude Code](https://github.com/anthropics/claude-code) - 强大的 AI 开发环境
- 社区贡献者的反馈和建议

---

**最后更新**: 2026-01-30
**文档版本**: 1.0.0
