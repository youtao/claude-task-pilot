# Claude Task Pilot - AI 驱动的任务管理系统

## 📖 项目概述

**Claude Task Pilot** 是一个专为 Claude Code 设计的自动化任务管理插件，通过文件系统监控和智能推荐，为 AI 驱动的开发流程提供无缝的任务追踪和状态管理。

### 核心特性

- **AI 原生设计**: 文档结构化、高信息密度，优化 Claude Code 的理解能力
- **手动触发**: 通过命令精确控制何时执行操作，避免不稳定的自动触发
- **智能推荐**: 基于优先级、依赖关系和里程碑自动推荐下一个任务
- **设计文档转换**: 手动将设计文档转换为可执行的任务卡片
- **快速启动**: 5-10 秒快速了解当前项目状态
- **文档驱动**: 基于文件系统的简单、透明设计

---

## 🏗️ 项目结构

```
claude-task-pilot/
├── .claude-plugin/
│   └── plugin.json              # 插件配置清单
├── agents/
│   ├── task-suggester.md        # 智能任务推荐代理
│   ├── design-to-tasks.md       # 设计文档转换代理
│   └── daily-reporter.md        # 日报生成代理
├── commands/
│   ├── setup-task-pilot.md      # 初始化命令
│   └── sync-progress.md         # 同步进度 + 完成任务 + 转换设计
├── templates/
│   ├── session.md               # Session 状态模板
│   ├── roadmap.md               # 长期路线图模板
│   ├── current-sprint.md        # 当前冲刺模板
│   ├── archive-index.md         # 归档索引模板
│   ├── task-complete.md         # 任务完成报告模板
│   └── task-backlog.md          # 任务卡片模板
├── CLAUDE.md                     # 本文档
├── README.md                     # 用户文档
└── .gitignore                    # Git 忽略规则
```

---

## 🎯 核心概念

### 文档结构

插件在用户项目中创建以下结构：

```
docs/
├── session.md              # 当前 Session 状态（<80 行）
├── plans/                  # 设计文档目录 (NEW)
│   └── YYYY-MM-DD-*-design.md
├── todo/
│   ├── roadmap.md          # 长期路线图（3-6 个月）
│   ├── current-sprint.md   # 当前冲刺（1-2 周）
│   └── backlog/            # 任务卡片
│       └── task-XXX-*.md
└── done/                   # 已完成任务归档
    ├── archive-index.md    # 归档索引
    └── YYYY-MM/            # 按月归档
        └── task-XXX-*.md
```

### 任务卡片格式

```markdown
# task-003: 补充番茄品种数据

**创建时间**: 2026-01-30
**优先级**: P0
**模块**: backend
**里程碑**: Phase 2: AI 核心功能

## 任务描述
补充 50 个常见番茄品种数据...

## 验收标准
- [ ] 至少 50 个品种
- [ ] 每个品种包含完整信息

## 依赖任务
- task-002: 收集番茄种植专业资料 ✅
```

### 优先级系统

- **P0**: 紧急重要，必须立即处理
- **P1**: 重要但不紧急，本周内完成
- **P2**: 可以延后，有空时处理

---

## 🔧 架构设计

### 命令执行流程

```
/sync-progress
    ↓
检测并修复文档不一致
    ↓
├─ quick: 快速同步
├─ full: 完全同步（更新 roadmap.md）
└─ verify: 验证模式

/sync-progress --complete-task
    ↓
归档任务
    ↓
├─ 更新 session.md、current-sprint.md
├─ 归档关联设计文档
└─ 推荐下一个任务

/sync-progress --convert-design
    ↓
转换设计文档
    ↓
├─ 调用 design-to-tasks agent
└─ 生成任务卡片到 backlog/
```

### Agent 调用

#### task-suggester
**触发时机**:
- 使用 `/sync-progress --complete-task` 完成任务后
- 用户主动询问

**推荐逻辑**:
1. 优先级评分（P0: 100, P1: 70, P2: 40）
2. 依赖关系检查（依赖任务必须完成）
3. 里程碑对齐（当前里程碑优先）
4. 模块平衡（避免连续做同一模块）

#### design-to-tasks
**触发时机**:
- 使用 `/sync-progress --convert-design` 时

**转换逻辑**:
1. 分析设计文档内容
2. 按模块拆分任务（backend/frontend/testing/documentation）
3. 自动判断优先级
4. 识别依赖关系

#### daily-reporter
**触发时机**:
- 用户主动调用

**报告内容**:
- 昨日完成任务
- 今日推荐任务
- 本周进度概览

---

## 💡 设计原则

### 1. AI 优先
- 文档使用结构化 Markdown
- 高信息密度，减少 Claude 的 token 消耗
- 明确的状态标记（✅、🔄、⏳）

### 2. 快速启动
- session.md 限制 80 行，快速读取
- 关键信息在前 20 行
- 5-10 秒了解项目状态

### 3. 任务驱动
- 所有开发活动围绕任务卡片
- 任务卡片是真理的唯一来源
- 完成即归档，不可修改

### 4. 完整追溯
- 任务生命周期完整记录
- 按月归档，便于回顾
- archive-index.md 提供全局视图

### 5. 手动触发
- 通过命令精确控制操作时机
- 避免不稳定的自动触发
- 失败时不影响正常开发

---

## 🚀 使用指南

### 安装

```bash
# 克隆到插件目录
cd ~/.claude/plugins
git clone https://github.com/your-username/claude-task-pilot.git

# 或手动复制
cp -r claude-task-pilot ~/.claude/plugins/
```

### 配置

**项目级配置** (`.claude/claude-task-pilot.local.md`):

```yaml
---
docs_root: "docs"
session_max_lines: 80
---
```

### 初始化项目

使用命令初始化新项目：

```bash
/setup-task-pilot
```

将会创建:
- docs/session.md
- docs/todo/roadmap.md
- docs/todo/current-sprint.md
- docs/done/archive-index.md
- docs/todo/backlog/
- docs/done/YYYY-MM/

### 创建任务

在 `docs/todo/backlog/` 创建新任务卡片：

```bash
# 示例
cat > docs/todo/backlog/task-004-design-api.md << 'EOF'
# task-004: 设计 REST API

**创建时间**: 2026-02-04
**优先级**: P1
**模块**: backend

## 任务描述
设计用户认证和数据管理的 REST API...

## 验收标准
- [ ] API 文档完整
- [ ] 包含错误处理
- [ ] 通过安全审查
EOF
```

创建后，使用同步命令更新文档状态：

```bash
/sync-progress
```

### 完成任务

使用命令标记任务完成并归档：

```bash
/sync-progress --complete-task task-004
```

或使用文件名：

```bash
/sync-progress -c task-004-design-api.md
```

命令将自动：
- ✅ 归档任务到 `docs/done/YYYY-MM/`
- ✅ 更新 `session.md`
- ✅ 更新 `current-sprint.md`
- ✅ 更新 `archive-index.md`
- ✅ 归档关联的设计文档（如果存在）
- ✅ 推荐下一个任务

### 转换设计文档

将设计文档转换为任务卡片：

```bash
# 使用最新设计文档
/sync-progress --convert-design

# 或指定设计文档
/sync-progress -d docs/plans/2026-02-04-feature-design.md
```

### 同步进度

定期同步所有文档：

```bash
# 快速同步（默认）
/sync-progress

# 完全同步（更新 roadmap.md）
/sync-progress full

# 验证模式（不修改文件）
/sync-progress verify
```

---

## 🛠️ 开发指南

### 添加新 Agent

1. 在 `agents/` 目录创建 `.md` 文件
2. 定义触发条件和分析逻辑
3. 在需要时通过命令调用

### 修改模板

模板文件位于 `templates/` 目录，修改后自动生效。

建议：
- 保持简洁，信息密度高
- 使用明确的 emoji 标记
- 包含使用示例

---

## 📦 版本管理

### 版本号同步

每次发布新版本时，需要同步更新以下文件中的版本号：

1. **`.claude-plugin/plugin.json`**
   ```json
   {
     "version": "1.2.0"
   }
   ```

2. **`.claude-plugin/marketplace.json`**
   ```json
   {
     "version": "1.2.0",
     "plugins": [
       {
         "version": "1.2.0"
       }
     ]
   }
   ```

3. **`skills/*/SKILL.md`** (如果有 skill)
   ```yaml
   ---
   name: skill-name
   description: This skill should be used when...
   version: 1.2.0
   ---
   ```

4. **`README.md`** 中的版本徽章
   ```markdown
   [![版本](https://img.shields.io/badge/版本-1.2.0-blue.svg)]
   ```

5. **Git Tags**
   ```bash
   git tag -a v1.2.0 -m "Release notes"
   git push origin v1.2.0
   ```

### 版本发布流程

完整的版本发布流程：

```bash
# 1. 更新版本号
# 编辑 plugin.json、marketplace.json、README.md

# 2. 提交更改
git add .
git commit -m "chore: bump version to 1.1.0"

# 3. 创建 tag
git tag -a v1.1.0 -m "Release v1.1.0"

# 4. 推送代码和标签
git push origin master
git push origin v1.1.0

# 5. 创建 GitHub Release
gh release create v1.1.0 --title "v1.1.0" --notes "Release notes..."
```

### 版本号规范

遵循 **语义化版本 (Semantic Versioning)**：

- **MAJOR.MINOR.PATCH** (例如：1.1.0)
  - **MAJOR**: 重大功能变更或破坏性更新
  - **MINOR**: 新增功能（向后兼容）
  - **PATCH**: Bug 修复或小改进

示例：
- `1.0.0` → `1.0.1`: 修复 bug
- `1.0.1` → `1.1.0`: 添加新功能
- `1.1.0` → `2.0.0`: 重大架构调整

### 发布前检查清单

在发布新版本前，确保：

- [ ] 更新 `.claude-plugin/plugin.json` 版本号
- [ ] 更新 `.claude-plugin/marketplace.json` 版本号（两处）
- [ ] 更新 `skills/*/SKILL.md` 版本号（如果有 skill）
- [ ] 更新 `README.md` 版本徽章
- [ ] 更新 `README.md` 更新日志
- [ ] 运行所有测试场景
- [ ] 更新 CLAUDE.md 版本历史（如有重大变更）
- [ ] 创建 Git tag
- [ ] 创建 GitHub Release
- [ ] 推送所有更改到远程仓库

---

## 🧪 测试

### 手动测试场景

```bash
# 1. 新项目初始化
cd /tmp/test-project
claude-code
# 应该提示初始化

# 2. 创建任务
cat > docs/todo/backlog/task-001.md
# 检查 session.md 是否自动更新

# 3. 完成任务
mv docs/todo/backlog/task-001.md docs/done/2026-01/
# 检查是否生成完成报告和推荐

# 4. 重建 session.md
rm docs/session.md
claude-code
# 应该自动重建
```

### 自动化测试（待实现）

```bash
npm test
```

---

## 📊 性能指标

- **启动时间**: < 100ms
- **推荐计算**: < 500ms
- **文件监控**: 零延迟（基于 Hooks）
- **内存占用**: 最小化（无持久化进程）

---

## ⚠️ 注意事项

### 限制

1. **session.md 行数限制**: 超过 80 行会自动归档旧任务
2. **文件格式**: 必须使用 Markdown 格式
3. **目录结构**: 不能自定义目录结构（可通过配置修改路径）
4. **并发处理**: 不支持多个 Claude Code 实例同时写入

### 已知问题

- [ ] 移动任务卡片后，如果 Hook 未触发，需要手动更新索引
- [ ] 依赖关系解析基于字符串匹配，可能出现误判
- [ ] 模块平衡逻辑过于简单

---

## 🎨 设计文档转换

### 功能概述

自动将设计文档（`docs/plans/YYYY-MM-DD-*-design.md`）转换为可执行的任务卡片，无缝集成 brainstorming workflow。

### 触发方式

#### 1. 自动触发（Hook）

**条件**: 设计文档创建到 `docs/plans/` 目录

**配置**:
```yaml
# .claude/claude-task-pilot.local.md
auto_convert_designs: true        # 启用自动转换
prompt_before_convert: false      # 不询问，直接生成（默认）
```

**流程**:
1. 用户完成 brainstorming，设计文档创建
2. PostToolWrite Hook 检测到设计文档
3. **自动调用** design-to-tasks agent
4. 自动生成任务卡片到 `docs/todo/backlog/`

**可选：启用确认提示**:
```yaml
prompt_before_convert: true       # 转换前询问用户
```

#### 2. 手动触发（命令）

```bash
# 使用最新设计文档
/convert-design-to-tasks

# 指定设计文档
/convert-design-to-tasks docs/plans/2026-02-01-user-auth-design.md
```

### 任务生成逻辑

#### 按模块拆分

- **Backend**: 数据模型、API、业务逻辑
- **Frontend**: 页面组件、UI 元素、状态管理
- **Infrastructure**: 数据库、配置、部署
- **Testing**: 单元测试、集成测试、E2E 测试
- **Documentation**: API 文档、用户手册

#### 优先级自动判断

**评分算法**:
```javascript
总分 = 复杂度(30%) + 依赖数量(25%) + 模块关键性(25%) + 用户价值(20%)

P0: ≥70 分（关键路径、基础设施）
P1: ≥50 分（主要功能、用户可见）
P2: <50 分（辅助功能、文档）
```

#### 依赖关系识别

- 数据模型 → API 依赖
- API → 前端组件 依赖
- 功能 → 测试 依赖
- 基础设施 → 所有模块 依赖

### 任务卡片格式

生成的任务卡片包含：

```markdown
# task-XXX: 任务名称

**创建时间**: YYYY-MM-DD
**优先级**: P0/P1/P2
**模块**: backend/frontend/infrastructure/testing/documentation
**里程碑**: Current Sprint
**相关设计**: docs/plans/YYYY-MM-DD-*-design.md

## 任务描述
...

## 验收标准
- [ ] 标准1
- [ ] 标准2

## 依赖任务
- task-YYY: ... ⏳

## 相关文件
...

## 预计时间
X 小时
```

### 配置选项

**`.claude/claude-task-pilot.local.md`**:
```yaml
---
docs_root: "docs"
session_max_lines: 80
default_priority: "P1"
default_milestone: "Current Sprint"
---
```

### 使用示例

#### 示例 1: 转换设计文档

```bash
# 1. 完成 brainstorming，设计文档已创建
# 2. 手动触发转换
/sync-progress --convert-design

# 3. 查看生成的任务
cat docs/todo/current-sprint.md
```

#### 示例 2: 完成任务并同步

```bash
# 1. 完成任务
/sync-progress --complete-task task-001

# 2. 完全同步所有文档
/sync-progress full
```

#### 示例 3: 组合操作

```bash
# 转换设计文档后立即完全同步
/sync-progress --convert-design --full

# 完成任务并验证
/sync-progress -c task-001 --verify
```

### 故障排查

**问题**: 命令无法找到设计文档

```bash
# 检查设计文档路径
ls -la docs/plans/

# 确保文件名符合格式：YYYY-MM-DD-*-design.md
```

**问题**: 任务卡片未生成

```bash
# 检查 backlog 目录是否存在
ls -la docs/todo/backlog/

# 手动创建目录
mkdir -p docs/todo/backlog
```
```

### 故障排查

**问题**: 插件未加载

```bash
# 检查插件目录
ls ~/.claude/plugins/claude-task-pilot

# 检查 plugin.json 语法
cat ~/.claude/plugins/claude-task-pilot/.claude-plugin/plugin.json | jq
```

**问题**: Hook 未触发

```bash
# 检查 Hook 文件格式
head -5 hooks/session-start.md

# 应该看到 YAML frontmatter
```

**问题**: Agent 调用失败

- 检查 Agent 文件是否存在
- 检查 `plugin.json` 中的 agent 声明
- 查看 Claude Code 日志

---

## 🔄 版本历史

### v2.0.0 (2026-02-04)

**重大变更**：
- 🔧 移除所有 hooks（改为手动命令触发，提高稳定性）
- ✨ 将 complete-task 功能集成到 sync-progress
- ✨ 将 convert-design-to-tasks 功能集成到 sync-progress
- 📝 精简 commands 为 2 个（setup-task-pilot + sync-progress）

**改进内容**：
1. **更稳定**：移除不稳定的 hooks，避免频繁失效
2. **更灵活**：用户可精确控制何时执行操作
3. **更强大**：sync-progress 支持组合操作（完成任务 + 同步）

**Breaking Changes**：
- 不再支持自动触发，所有操作需要手动执行命令
- `/complete-task` 命令已移除，使用 `/sync-progress --complete-task` 代替
- `/convert-design-to-tasks` 命令已移除，使用 `/sync-progress --convert-design` 代替

### v1.5.0 (2026-02-01)

**新增功能**：
- ✨ 设计文档自动转换（design-to-tasks agent）
- ✨ 自动检测设计文档创建并生成任务卡片
- ✨ 手动转换命令 `/convert-design-to-tasks`
- ✨ 智能任务拆分（按模块、复杂度）
- ✨ 优先级自动判断算法
- ✨ 依赖关系自动识别

**改进内容**：
1. **与 brainstorming 无缝集成**：
   - 设计文档创建后自动触发转换
   - 支持 superpowers:brainstorming 输出
   - 配置化控制自动/手动模式

2. **智能任务生成**：
   - 按模块拆分（backend/frontend/testing/doc）
   - 多维度优先级评分（复杂度、依赖、模块关键性、用户价值）
   - 自动识别依赖关系（数据模型→API→前端→测试）

3. **灵活配置**：
   - 支持自动/手动触发
   - 可配置是否询问用户
   - 可自定义优先级权重

### v1.0.0 (2026-01-30)

**初始版本**
- ✅ 自动初始化
- ✅ 任务状态追踪
- ✅ 智能任务推荐
- ✅ 日报生成
- ✅ 规范守护

### 计划功能

- [ ] Web Dashboard
- [ ] 任务时间追踪
- [ ] 团队协作支持
- [ ] GitHub Issues 集成
- [ ] 自定义评分算法

---

## 🤝 贡献指南

欢迎贡献！请遵循以下流程：

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'Add amazing feature'`)
4. 推送到分支 (`git push origin feature/amazing-feature`)
5. 开启 Pull Request

### 代码规范

- 所有文档使用 Markdown
- Hook 文件必须包含 YAML frontmatter
- Agent 文件必须包含示例对话
- 模板文件必须简洁明了

---

## 📄 许可证

MIT License - 详见 LICENSE 文件

---

## 👥 维护者

**youtao** - GitHub: [@your-username](https://github.com/your-username)

---

## 🙏 致谢

- [Claude Code](https://github.com/anthropics/claude-code) - 强大的 AI 开发环境
- 社区贡献者的反馈和建议

---

**最后更新**: 2026-02-01
**文档版本**: 1.5.0
